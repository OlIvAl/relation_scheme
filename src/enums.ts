export enum Roles {
  MEMBER_ROLE,
  GOVERNMENT_ROLE,
  CONVOCATION_ROLE,
  LIQUIDATOR_ROLE,
  PROCURATOR_ROLE
}

export enum Types {
  PRIVATE_PERSON,
  LEGAL_ENTITY
}

export enum Relations {
  CHILD = 1,
  PARENT = -1
}